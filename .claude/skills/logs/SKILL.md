---
name: logs
description: Manage implementation phase logging — on/off/clear/status
user_invocable: true
---

# Logs

Run `scripts/manage-logs.sh` with the argument the user provided (if any).
If no argument, run it with no arguments (defaults to `status`).

Report the script's output to the user.
